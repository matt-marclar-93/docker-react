sudo: required
language: generic 

services:
  - docker

before_install:
  - docker build -t mmarbury/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true mmarbury/docker-react npm run test